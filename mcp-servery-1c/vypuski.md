# Выпуски beta-образов

Что вошло в beta-сборки CodeMetadataSearchServer, Graph Metadata Search, SyntaxCheckServer, HelpSearchServer, SSLSearchServer и TemplatesSearchServer, опубликованные с 9 сентября 2026 года (SSLSearchServer и TemplatesSearchServer — с 24 сентября) (сборки перечислены по дате публикации; между ними теги не двигались). Теги `latest-beta`, `light-beta` и `arm64-beta` одного выпуска собираются из одного и того же кода; отличаются они только составом зависимостей (см. [Каналы образов](kanaly-obrazov.md)). Сборка опознаётся по digest образа: первые 12 символов `sha256` показывает `docker images --digests`, а полный digest — `docker image inspect <образ> --format '{{index .RepoDigests 0}}'`.

Правила, общие для всех выпусков ниже:

* **Индексы и графы открываются как есть.** Кроме HelpSearchServer от 23.09.2026 и выпусков 24.09.2026, которые один раз пересобирают индексы с префиксом документов, а также выпусков, которые один раз перестраивают часть данных: HelpSearchServer от 25.09.2026 (поздний вечер) полностью переиндексирует справку, Graph от 25.09.2026 (вечер) пересобирает граф вызовов, Graph от 25.09.2026 (поздний вечер) перезагружает метаданные и права ролей, Code от 26.09.2026 перестраивает структурные индексы (см. их строки), ни один сентябрьский выпуск не меняет формат хранилища zvec, отпечаток индекса или схему графа так, чтобы потребовалась полная переиндексация. Если сборка перечитывает что-то один раз (например, формы графа), это сказано в её строке.
* **Порядок обновления серверов не важен.** Code и Graph не зависят друг от друга: у каждого свои данные и свой лицензионный ключ; обновляйте их по одному в любом порядке.
* **Переменные окружения только добавляются.** Исключение — переменные префиксов эмбеддингов `EMBEDDING_QUERY_PREFIX`, `EMBEDDING_DOCUMENT_PREFIX` и `EMBEDDING_PASSAGE_PREFIX`: с 24.09.2026 они не применяются (заданная переменная выводит предупреждение в журнал). Остальные не переименованы и не удалены; новые перечислены в строке выпуска и на странице конфигурации сервера.

## CodeMetadataSearchServer (`comol/1c_code_metadata_mcp`)

| Дата | light-beta | Что изменилось |
|------|------------|----------------|
| 26.09.2026 | `7daf290b6e6a` (latest `4885214105af`, arm64 `100fb2974421`) | Дорожка, которую прерванное слияние оставило неслиянной (`fts_index_incompatible` / `rebuild_required`), восстанавливается следующим обычным прогоном из сохранённых в ней документов — без разбора исходников и эмбеддингов, даже на неизменной выгрузке; `reindex(force=true)` нужен, только если это не удалось. Обычный прогон больше не пересобирает повреждённую дорожку из исходников. В ту же сборку вошло всё из 25.09: бюджет ответа по умолчанию, права ролей с RLS, виртуальные таблицы и стандартные реквизиты, XDTO `Package.bin`, вызовы обработчиков по имени. |
| 26.09.2026 | `7a5403b45ceb` (latest `40e22b76b0ef`, arm64 `5d06dcf65cfc`) | Ответ без `max_chars` ограничен 32768 символами (`RESPONSE_MAX_CHARS`, `0` снимает предел): большой ответ приходит первой страницей с `truncated: true` и `next_cursor`; `get_form_artifact`, `get_report_artifact` и `get_role_artifact` листаются тем же курсором (`max_chars`, `max_items`, `detail_level`, `cursor`). `get_metadata_details`: карточка справочника, документа и регистра содержит `standard_attributes` — стандартные поля, которые у объекта действительно есть, выведенные из его свойств; карточка регистра — `virtual_tables`: таблицы запроса (`Остатки`, `Обороты`, `СрезПоследних`, `ОборотыДтКт`, `База<Регистр>`…) с параметрами и именами полей (`КоличествоОстаток`, `СуммаОборотДт`, `Субконто1`); таблиц, которых у регистра нет, в ответе нет. `get_role_artifact`: право на реквизит или табличную часть сохраняет свой путь (`Справочники.X.Реквизиты.Y`, `owner_path`) и не выдаётся за право на объект; выданное право несёт `conditional` и `restriction_ref`, тексты условий RLS и шаблоны ограничений роли — `restrictions=conditions|templates|all` постранично. XDTO-пакеты выгрузки конфигуратора показывают типы (`Ext/Package.bin` читается как XML). Обработчики, заданные строкой (`ОписаниеОповещения("Имя", …)`, `ПодключитьОбработчикОжидания("Имя")`), стали связями вызова. **Первый старт после обновления один раз** перестраивает структурные индексы (версия парсера 3); эмбеддинги не перестраиваются. |
| 25.09.2026 | `103a439043ef` (latest `d042f8792aef`, arm64 `2c0074bbee81`) | Объекты, которые добавляет расширение (отчёты, справочники, документы; выгрузка конфигуратора и EDT), попадают в коллекцию метаданных: `metadatasearch(origin="extension")`, в том числе с `names_only=true`, их находит. Заимствованные объекты по-прежнему описывает основная конфигурация. В режиме `names_only` строка больше не теряет путь файла, из-за чего фильтр `origin` считал любое имя базовым. На следующем прогоне индексируются только новые дескрипторы расширений. |
| 24.09.2026 | `cbd907ff06d9` (latest `5297d7bb2b57`, arm64 `5de9bacc0a2e`) | Текст эмбеддится без префиксов: из профилей моделей убраны `query: `/`passage: ` семейства e5, `document: ` у `qwen/qwen3-embedding-8b` и промпты EmbeddingGemma, `EMBEDDING_QUERY_PREFIX` и `EMBEDDING_DOCUMENT_PREFIX` не применяются. Запрос к Qwen3-Embedding по-прежнему идёт с инструкцией из карточки модели. **Индекс, документы которого записаны с префиксом** (модели e5, EmbeddingGemma, `qwen/qwen3-embedding-8b` или заданный `EMBEDDING_DOCUMENT_PREFIX`), **при первом запуске один раз пересобирается целиком** в новом поколении; прежнее отвечает до публикации нового. Остальные индексы не пересчитываются. |
| 23.09.2026 (поздний вечер) | `f4a4497f2c18` (latest `4633b478dabd`, arm64 `00fd13e99ea9`) | Запросы к модели Ollama `qwen3-embedding:<размер>` (`:8b`, `:4b`, `:0.6b`, `:latest`) идут с инструкцией задачи Qwen3, как у остальных имён Qwen3-Embedding; раньше они шли без неё. Документы эмбеддятся как раньше, переиндексация не нужна. Текст инструкции остался из карточки модели: формулировки под 1С на тестовом корпусе оказались хуже (см. [конфигурацию](servery/code-metadata-search/konfiguraciya.md)). |
| 23.09.2026 (вечер) | `01ad979b70ce` (latest `1b337233579d`, arm64 `43e7e34b46d9`) | Смена `EMBEDDING_MODEL` (или профиля векторного хранилища) больше не останавливает сервер при запуске ошибкой `zvec store signature mismatch`. Сервер стартует, векторы старой модели в поиске не участвуют (поиск по коду в это время идёт по файлам, журнал называет каждую дорожку и отличающуюся настройку), полный индекс новой модели собирается в новом поколении и включается после публикации; `RESET_DATABASE` не нужен. Индекс, построенный той же моделью, открывается как есть, переиндексация не нужна. |
| 23.09.2026 | `fcd16e372f45` (latest `246705024ebe`, arm64 `889c4a18865a`) | Описание инструмента `search_forms` называет фактический поиск — гибридный (вектор + полнотекстовый, RRF, переранжирование) по коллекции `forms` индекса zvec — вместо ChromaDB, и точное условие перехода на поиск по файлам. Поведение и индекс не менялись, переиндексация не нужна. |
| 22.09.2026 | `4e2dcc1208f9` (latest `53013da95150`, arm64 `2d17730f8223`) | Запросы к Qwen3-Embedding (0.6B, 4B, 8B) идут с инструкцией задачи из карточки модели вместо `query: `; на тестовом корпусе у 8B MRR 0,855 → 0,941. Префиксы документов прежние, переиндексация не нужна. Добавлен профиль EmbeddingGemma (`task: search result \| query: ` / `title: none \| text: `); индекс, построенный на ней раньше, пересоберите. |
| 19.09.2026 | `747b60e559cf` (latest `357eabfeeaab`, arm64 `1b9e97fd6848`) | Глубина поиска HNSW `ef` ограничена пределом zvec 2048: поиск с фильтром по области, расширяющий окно кандидатов, больше не теряет векторную полосу молча (ошибка backend теперь пишется в журнал как WARNING). `build_ordinary_form` пишет записи `Form.bin` открытым текстом, что бы ни говорил флаг `deflated` (платформа 8.3.27 не открывает сжатые потоки). Индексация, формат журнала эмбеддингов и слияния не затронуты; переиндексация не нужна. |
| 18.09.2026 (вечер) | `aeae0e0eaee5` (latest `24bd2d4f21aa`, arm64 `8e9956a7bb04`) | Журнал эмбеддингов (`embedding-journal/`) пишет float32 вместо float64 — вдвое меньше на запись; старые записи читаются и заменяются по мере изменения файлов. Новая переменная `EMBEDDING_JOURNAL=false` выключает журнал. Раздел «Место на диске» в документации: ориентиры по поколениям и журналу. |
| 18.09.2026 | `57b5f05214e7` (latest `27f30817ce2c`, arm64 `5354fcf2c6aa`) | zvec 0.7.0 на всех платформах: запись в хранилище идёт во время фонового слияния, хвост фазы кода больше не стоит часами на последних файлах. Журнал говорит, чего ждёт фаза (`waiting for optimize`), и ведёт темп по последним минутам. Старые хранилища открываются без пересборки. |
| 16.09.2026 | `6e372b5b13f1` (latest `e31f5108df36`, arm64 `1d50828023bf`) | Отпечаток `embedding_model` не зависит от прогрева процесса: перезапуск контейнера с той же удалённой моделью больше не запускает полную пересборку. XML расширений в подындексном проходе стоит одного поиска, а не сканирования хранилища. |
| 11.09.2026 | `d8f4300f04ed` (latest `0a3eb69d5a35`, arm64 `14fe74abefb2`) | Полнотекстовый индекс не пересобирается после каждого XML-описания объекта расширения (отложенная сборка FTS). |
| 11.09.2026 | `40853885bda9` (latest `89ecc66a9184`, arm64 `822e39833462`) | Обход SIGSEGV во втором нативном FTS-запросе zvec по одному хранилищу: гибридный поиск идёт лексикой, затем вектором, в одном потоке. Полностью причина (AVX2 в собственной сборке zvec) устранена 14.09 в следующей сборке. |
| 09.09.2026 | `c46dd0e4a420` (latest `3d1359788fc2`, arm64 `5b4f50315201`) | Хенг фазы кода на 98 %: периодический flush zvec больше не вызывается во время фонового `optimize` (все потоки засыпали на блокировке записи). |
| 09.09.2026 | `480e7cfb699c` (latest `6cd611301b16`, arm64 `53a2aa1a202a`) | `fts_index_incompatible` на инкрементальных прогонах: поколение копируется и публикуется только после финализации записи и FTS (с проверкой числа документов); перестраиваемый индекс не удаляется сборщиком мусора. |

## Graph Metadata Search (`comol/1c_graph_metadata`)

| Дата | light-beta | Что изменилось |
|------|------------|----------------|
| 25.09.2026 (поздний вечер) | `3ce5b127e6fd` (latest `807d3cf970ec`, arm64 `868d6251296f`) | `get_object_dossier`: стандартные реквизиты, которые у объекта действительно есть (выводятся из его свойств: `Код`/`Наименование` по длине, `Владелец` по владельцам, `Период` только у периодического регистра сведений и т. д.), и виртуальные таблицы регистров (`.Остатки`, `.Обороты`, `.СрезПоследних`, `ОборотыДтКт`, `ДанныеГрафика`, `База…`) с параметрами; поля таблиц с производными именами (`КоличествоОстаток`, `СуммаОборотДт`, `Субконто1`) — `sections=["virtual_tables"]`. Имена по справке платформы 8.3.27.2130; таблиц, которых у регистра нет, в ответе нет, причина названа. `get_access_rights` отвечает объявленными правами ролей: `rights` — выдано, `denied_rights` — явно запрещено (раньше блок одних запретов терялся), `conditional_rights` — выдано с ограничением RLS (`restriction_ref`); тексты условий и шаблоны ограничений роли — `restrictions='conditions'|'templates'` постранично. **Первый старт после обновления один раз** пересинтезирует отчёт, повторно загружает метаданные и перечитывает права ролей (при `LOAD_ROLE_RIGHTS=true`); BSL, формы и эмбеддинги не перестраиваются. |
| 25.09.2026 (вечер) | `47ccc4324204` (latest `f01c810bd0b2`, arm64 `a2b202f9b227`) | `compare_graph_scope(extension_ref=…)` в режиме каталога: связи читаются с квотой на каждый тип связи, а не на все связи метки сразу, и неполным считается обрезанный тип связи — раньше заимствованные объекты с более чем 10 000 связей выбрасывали из сравнения все узлы MetadataObject (`incomplete_kinds=["MetadataObject"]`); концы связей передаются только ключами, именем и слоем — на проекте масштаба ЗУП сравнение без `node_kinds` около 10 с вместо 35–41. Обработчики, названные строкой (`ОписаниеОповещения("Имя", …)`, `ПодключитьОбработчикОжидания("Имя")`), дают рёбра `CALLS`; версия лейна `bsl_call_graph` 4→5, поэтому первый старт после обновления один раз пересобирает граф вызовов (на ЗУП около 20–25 мин). Остальные данные не пересобираются. |
| 25.09.2026 | `a9cee2a57e5c` (latest `61f7499d1da6`, arm64 `79e3ad948643`) | Статусы не выдают заполненный граф за пустой. `get_graph_stats`, `get_graph_schema` и `list_graph_indexes` больше не ставят `degraded` и не несут предупреждение о `GRAPH_SCOPE_ENFORCED` (факт установки остаётся в `installation_notices` у `list_graph_capabilities` и `health_graph`, текст переписан: это штатное хранение, а не остатки данных); `get_graph_stats` отвечает полями `data.scope_confined` и `data.evidence_recorded` (нулевые evidence при `REFERENCE_EVIDENCE_ENABLED=false` — норма). `list_graph_projects` показывает `inherited` с первого вызова, а не после детального; вложенный `ingestion.state` совпадает с `ingestion_state`; без координатора причина так и названа. В режиме graph-only `degraded` по-прежнему ставится на любой ответ. Данные и индексы открываются как есть. |
| 24.09.2026 (вечер) | `3f71e8391a89` (latest `52549a70e707`, arm64 `80d4690d25f7`) | Формы и модули одноимённых объектов разных видов больше не сливаются. Ключ формы и всех её элементов (у управляемых и обычных форм) включает вид владельца, а модуль привязывается (`HAS_MODULE`) только к объекту вида своей папки выгрузки. `find_form_links` и `get_form_structure` понимают вид в `object_name` (`Документ.РеестрДокументов`); на голое имя, которое носят объекты разных видов с этой формой, они отвечают `ambiguous` со списком `candidates`. `compare_base_and_extension` принимает вид и называет сравниваемый вид в заголовке. Первый старт на существующих данных дольше обычного: конфигурация перечитывается один раз, склеенные формы разделяются, структура всех форм перезаписывается (`FORM_XML_IDENTITY_VERSION=6`), лишние `HAS_MODULE` удаляются. Пересобирать базу не нужно. `NEO4J_READ_TRANSACTION_TIMEOUT_S` теперь ограничивает только запросы инструментов и веб-поиска, а фоновые дорожки читают без предела: на графе из ~200 тыс. процедур выборка для эмбеддингов обрывалась через 30 с. `refresh_graph_project(mode=full)` без манифеста исходных единиц отвечает `accepted` со `staging_generation` и продолжает пересборку на сервере. Ход и итог показывает `get_graph_project_status(operation_id=...)`, неудачу — `ingestion_state=failed` с `last_error`. |
| 24.09.2026 (день) | `45be453fbe4b` (latest `efc5352ddd87`, arm64 `71e9f0e5c9c6`) | Готовность векторной полосы в `/readyz`, `get_graph_project_status` и `health_graph` считается по индексам тех семантических полос, которые включены: бизнес-поиск — `business_info_vector_index` или `business_chunk_vector_index`, поиск по коду — `routine_vector_index` или `code_vector_index`, описания метаданных — `description_vector_index` или `description_chunk_vector_index`, эмбеддинги процедур — `routine_vector_index`. Раньше проверялся только `business_info_vector_index`, поэтому установка с выключенным бизнес-поиском оставалась в `ready_structural` (`vector=false`, `hybrid=false`) при готовом `routine_vector_index`. Если не включена ни одна семантическая полоса, `vector=false`. Индекс, которого не было при первой проверке, больше не считается отсутствующим до перезапуска: `/readyz` и поиск видят его, как только индексация его построила. Индексы и граф открываются как есть, переиндексация не нужна. |
| 24.09.2026 (утро) | `7bf3681fd371` (latest `54d4b4b27a93`, arm64 `69fd9a007e61`) | Только сборка: скомпилированные модули те же, что в выпуске 23.09 (вечер). Из образов убраны оставшиеся от компиляции пакеты Debian, из light — пакет Cython; в latest обновились незакреплённые зависимости (`openai` 3.19.2, `cyclopts` 5.0.0 и др.). Поведение сервера не менялось. |
| 23.09.2026 (вечер) | `524f0b4adc27` (latest `ea786061ae98`, arm64 `7d391975b8b0`) | Запрос к модели Qwen3-Embedding (`qwen/qwen3-embedding-8b` по умолчанию, `qwen3-embedding:8b` в Ollama) идёт с инструкцией задачи, сформулированной под объекты метаданных и код 1С (см. [Инструкция к запросу у Qwen3](embedding-modeli/vybor-modeli.md#инструкция-к-запросу-у-qwen3)); у других моделей запрос не меняется. Узлы графа эмбеддятся как раньше: отпечатки и версии лейнов не меняются, переэмбеддинга нет. |
| 23.09.2026 | `06cfffd5cc9f` (latest `015b749e811c`, arm64 `0707c4e57a64`) | Чтение из Neo4j ограничено `NEO4J_READ_TRANSACTION_TIMEOUT_S` (новая переменная, 30 с): брошенный после таймаута инструмента запрос прерывается сервером Neo4j и не держит соединение. Переподключение к Neo4j выполняется под блокировкой и один раз на сбой, по типу ошибки (`ServiceUnavailable`/`SessionExpired`), а не по тексту. Префикс категории в `object_name`/`register_name` (`Справочник.X`, `Catalog.X`, `РегистрНакопления.X`) у `find_objects_using_object`, `find_usages_of_object`, `find_register_movement_docs` и `trace_impact` фильтрует по категории — одноимённые объекты разных категорий больше не смешиваются. `find_by_guid` ищет по индексам GUID вместо сканирования всех узлов и не выводит эмбеддинги. `find_usages_of_object` и `find_objects_using_object` пагинируются только контрактными `max_items`/`cursor` (чтение ограничено 25 × `GRAPH_MAX_ITEMS`, обрезка — в `warnings`); `limit`/`offset` у `find_usages_of_object` приняты, но не применяются и помечены в `deprecated`. `compare_graph_scope(extension_ref)` в режиме каталога больше не упирается в таймаут: связи базы читаются по узлам пар, а не сканированием метки на каждый ключ (40 с → ~4 с на 197 тыс. процедур). Новые связи BSL-кода (запись движений, `READS_TABLE`, вызовы глобальных модулей, динамические вызовы, `EXECUTES` регламентных заданий, `read_status = protected`) — см. [страницу сервера](servery/graph-metadata-search/README.md), раздел «Связи BSL-кода»; при первом старте лейны `bsl_call_graph` (3 → 4) и `bsl_register_access` (1 → 2) один раз пересобираются на месте, узлы и эмбеддинги не трогаются, переиндексация не нужна. |
| 22.09.2026 | `df01dc527859` (latest `053f042e8d0e`, arm64 `08812242f2ce`) | Чтение во время публикации поколения (`refresh_graph_project(mode=full)`, промоушен под координатором): публикация идёт пачками, и раньше чтение в этом окне могло вернуть пустой или частичный проект как достоверный; теперь публикация объявляет о себе, чтение ждёт её окончания (`GRAPH_GENERATION_FENCE_WAIT_S`, 120 с) или отвечает `generation_fence_exhausted`; брошенная публикация старше `GRAPH_GENERATION_FENCE_STALE_S` (900 с) читается как есть. Проект, у которого вручную удалили строки трекера индексации, снова берёт лизу (раньше — «Another ingestion writer keeps taking the lease» на каждом старте). Переиндексация не нужна. |
| 21.09.2026 | `fda12ebf500b` (latest `79fbff279338`, arm64 `99c800b0d4e1`) | Режим каталога расширений: лейн эмбеддингов процедур стартует после загрузки слоёв и добирает появившееся за время работы; маркеры resume графа вызовов — на слой (первый рестарт после холодного прогона больше не перестраивает граф вызовов базы; если маркер на момент обновления хранит штамп слоя — перестроится один раз); ремонт старого ключа объектов не срабатывает на одноимённых объектах двух слоёв; `find_form_links` отдаёт обработчики команд и ищет обработчики событий только в модуле своей формы (`FORM_XML_IDENTITY_VERSION=5`, формы перечитываются один раз на первом старте); прогон каталога читает права ролей слоя; `compare_graph_scope(extension_ref=…)` сравнивает слой с базой того же проекта без `EXTENSION_BASE_PROJECT`. Граф следует за выгрузкой без перезапуска: `GRAPH_REFRESH_INTERVAL_SEC` (3600; `0` выключает) / `GRAPH_REFRESH_APPLY`; статус в `get_indexing_status().source_refresh`. |
| 19.09.2026 | `ba1a7ac29ac6` (latest `dfe3cad3e79b`, arm64 `db13733704ac`) | `build_ordinary_form` пишет записи `Form.bin` открытым текстом (тот же кодек, что в Code). Граф не затронут. |
| 18.09.2026 (вечер) | `3b0894511039` (latest `d93d25c8e85e`, arm64 `c2345fcf2598`) | При `GRAPH_SCOPE_ENFORCED=true` создаются индексы `(mcp_namespace, project_id)` на каждую метку проекта — чтения, ограниченные только scope (в том числе `compare_graph_scope`), перестают сканировать всю базу; команда управляемой формы ключуется формой и её владельцем (`FORM_XML_IDENTITY_VERSION=4`, формы перечитываются один раз); настройки загружаемого слоя каталога — окно потока каталога, лейн форм базы читает базовую выгрузку. |
| 18.09.2026 | `d1fb1a9b2b65` (latest `6c0a6d04993e`, arm64 `8a853f60c7a5`) | Связи `EXTENDS`/`OVERRIDES` строятся до удаления рёбер прошлого прогона — пересоздание контейнера не оставляет граф без связей слоя; каждая транзакция записи ограничена `NEO4J_WRITE_TRANSACTION_TIMEOUT_S` (1800 с), затянувшаяся запись видна в журнале (`NEO4J_WRITE_STALL_WARNING_S`). |
| 17.09.2026 (вечер) | `4eadac271dce` (latest `125f4a15a8e7`, arm64 `0c2f188f4fc0`) | `list_graph_projects` показывает тот же `ingestion_state`, что и `get_graph_project_status` (`running`/`idle`/`failed` живого загрузчика; `idle` вместо `never_run` при известном поколении). |
| 17.09.2026 | `84e7006c2570` (latest `cff3fdfdbd42`, arm64 `098c3cf0b2c3`) | `compare_graph_scope(extension_ref=…)` находит базу по данным в Neo4j, а не только по реестру процесса, и читает с базовой стороны только объекты, связанные с расширением рёбрами `EXTENDS`/`OVERRIDES`. Бизнес-описания строятся и для объектов, у которых в выгрузке нет собственной папки (один `<Имя>.xml`). |
| 16.09.2026 (вечер) | `3a0d876e4bb1` (latest `51a04a029ede`, arm64 `762ac706af21`) | Структура управляемых форм ключуется конфигурацией-владельцем: реквизиты, команды и события форм снова создаются в scoped-режиме; формы перечитываются один раз. |
| 16.09.2026 | `4eb8a89ec158` (latest `97d4f0591426`, arm64 `c751a06b9708`) | `generate_business_info` начинает работу за секунды, а не часы (формат выгрузки определяется один раз за прогон) и не блокирует `vector_indexing`; временные ошибки провайдера эмбеддингов повторяются с нарастающей паузой; ход перечисления виден в журнале. |
| 11.09.2026 | `7c77133e7267` (latest `83116c57d1da`, arm64 `5fd04281f267`) | Одновременный `CREATE CONSTRAINT IngestionLease` базы и расширения на пустой Neo4j: транзиентный `DeadlockDetected` повторяется с той же паузой, что и остальные записи, а не роняет первичную индексацию. |
| 09.09.2026 | `49eff4b0b762` (latest `db3eaebcc726`, arm64 `01a7fd10302d`) | Объекты базы и расширений с одинаковым именем (`Report.txt`) больше не сливаются в один узел: ключ объектов и их детей включает `config_name`. |
| 09.09.2026 | `b04395335d41` (latest `174be4b87723`, arm64 `2a1e9c08772a`) | `CALCULATE_BUSINESS_INFO` работает на выгрузках 1C:EDT (пути Designer XML больше не обязательны). |

## SyntaxCheckServer (`comol/1c_syntaxcheck_mcp`)

| Дата | latest-beta | Что изменилось |
|------|-------------|----------------|
| 25.09.2026 | `a109f82dae79` (arm64 `81be2b17d168`) | Проверка имён платформы. Раньше без индекса конфигурации проходили без замечаний `Массив.ДобавитьЭлемент()`, `Структура.Ключи()`, `ВидДвиженияНакопления.Поступление` и `Новый НесуществующийТип`. Теперь сервер читает модель платформы у своего же анализатора (в фоне при старте, около 10 с; под эмуляцией arm64 — до 2 мин) и сообщает `UnknownPlatformMethod`, `UnknownPlatformProperty`, `UnknownSystemEnumValue` и `UnknownPlatformType` (`Critical`). Для `syntaxcheck` добавлен ещё `PlatformApiUnavailableOnClient` (`Major`): серверный тип или функция в методе `&НаКлиенте`. Готовность модели показывает `provenance.platform_names`. Вызовы без объекта (`КонецТранзакции()`) не проверяются. Размер ответа `syntaxcheck` / `syntaxcheck_file` ограничен `RESPONSE_MAX_CHARS` (по умолчанию 32768 байт, `0` снимает предел). Если находки не помещаются, остаются самые серьёзные, а `summary.truncated` равен `true`. Индекс открывается как есть. |
| 22.09.2026 | `286625bee89b` (arm64 `a8fd3aabe478`) | `syntaxcheck_file` в режиме FULLINDEX ждёт индекс не дольше 20 секунд: при зависшем построении графа (например, выгрузка на bind-mount с диска Windows) проверка отвечает без межмодульных находок вместо ожидания до отмены клиентом. Состояния индекса `failed`/`disabled` больше не считаются готовым индексом. Самообновление анализатора сверяет загрузку с размером и SHA-256 релиза и переустанавливает повреждённый бинарник. |

## HelpSearchServer (`comol/1c_help_mcp`)

| Дата | light-beta | Что изменилось |
|------|------------|----------------|
| 25.09.2026 (поздний вечер) | `5b1c11fbee26` (latest `d89f20b0cec5`, arm64 `4984a7e67cbf`) | В корпус синтакс-помощника вошёл раздел таблиц языка запросов (`tables/` архива), который отсекал фильтр путей: терялась 721 страница из 725. 59 таблиц (`Остатки`, `Обороты`, `ОстаткиИОбороты`, `СрезПервых`, `СрезПоследних`, `ДвиженияССубконто`, таблицы `Изменения` и др.) индексируются по одному документу с разделами «Поля» и «Параметры»: `docsearch("РегистрНакопления Остатки")` находит страницу таблицы. Цитата страницы синтакс-помощника несёт `since` (версия из «Использование в версии») и `availability` (окружения из «Доступность») в `compact` и `detailed`; контракт ответа `schema_version: "4.1"`, ключи только добавлены. `docinfo` с неоднозначным именем в режиме `compact` больше не падает с внутренней ошибкой. В стандартах `anti-patterns`, `registers-design`, `async-methods` дополнены разделы о параметре условия виртуальной таблицы, виртуальных таблицах по видам регистров и условном требовании асинхронных файловых вызовов. **Первый старт на существующем индексе один раз полностью переиндексирует справку** в staging-поколении; до конца сборки отвечает прежнее поколение. |
| 24.09.2026 | `17df95a1d6af` (latest `09dcd79ce4e6`, arm64 `fe63b129d853`) | Запрос к модели Qwen3-Embedding идёт с инструкцией задачи, сформулированной под справку 1С (см. [Инструкция к запросу у Qwen3](embedding-modeli/vybor-modeli.md#инструкция-к-запросу-у-qwen3)); у других моделей запрос не меняется. Из таблицы моделей убраны префиксы, которые сервер и раньше не применял, а только печатал в журнал при старте. Индекс не меняется, переиндексация не нужна. |
| 23.09.2026 (вечер) | `a976aff97dab` (latest `2fc57143e24e`, arm64 `dfb92b7e182d`) | Новая переменная `EMBEDDING_API_TIMEOUT` — сколько секунд один запрос к embedding API ждёт ответа (по умолчанию `600`, как раньше). Раньше предел был зашит в клиенте OpenAI, и модель на CPU, которая считает `INDEXING_WORKERS` батчей по очереди, не укладывалась в него: батч падал после трёх попыток по 600 с. Индекс не меняется, переиндексация не нужна; кто обновляется со сборки раньше 23.09 (утро), получит однократную переиндексацию из строки ниже. |
| 23.09.2026 | `77bf1efeb754` (latest `84d71392304c`, arm64 `ba16e68df231`) | В корпус синтакс-помощника вернулись около 626 страниц, которые отсекал фильтр служебных файлов архива (ОписаниеТипов, ЦветаСтиля, ШрифтыСтиля, WebЦвета.Серебряный, МенеджерПолученияЛицензии и др.): 23 897 → 24 523 страниц. **При первом старте на существующем индексе справка один раз полностью переиндексируется** в staging-поколении; до конца сборки отвечает прежнее поколение. Если embedding-модель не загрузилась (нет доступного API в light, нет модели в `HF_HOME`), сборка индекса завершается штатным отказом — `/ready` отвечает `degraded` с причиной в `failure` — вместо бесконечного `starting`; контейнер при этом не останавливается. |

## SSLSearchServer (`comol/mcp_ssl_server`)

| Дата | light-beta | Что изменилось |
|------|------------|----------------|
| 24.09.2026 | `2dbfa8e8a1d1` (latest `6edd8aa0ed82`, arm64 `6a2777ea0a2d`) | Текст эмбеддится без префиксов: локальная модель больше не получает `query: `/`passage: ` (e5), `search_query: `/`search_document: ` (nomic), инструкции bge/mxbai и встроенные prompts модели. Запрос к Qwen3-Embedding идёт с инструкцией задачи под БСП (см. [Инструкция к запросу у Qwen3](embedding-modeli/vybor-modeli.md#инструкция-к-запросу-у-qwen3)); `EMBEDDING_INPUT_TYPE_ENABLED=false` её выключает. **Коллекция, построенная локальной моделью e5** (модель полного образа по умолчанию), **nomic или моделью со встроенным prompt документа, при первом старте один раз пересобирается.** Коллекции API-режима и остальных моделей не пересчитываются. |

## TemplatesSearchServer (`comol/template-search-mcp`)

| Дата | light-beta | Что изменилось |
|------|------------|----------------|
| 24.09.2026 | `f754a0de6eec` (latest `b6e7436ce58e`, arm64 `c4ca2beb29fb`) | `EMBEDDING_QUERY_PREFIX` и `EMBEDDING_PASSAGE_PREFIX` больше не читаются: текст эмбеддится как есть. Запрос к Qwen3-Embedding идёт с инструкцией задачи: для `templatesearch` — под шаблоны кода, для `recall` — под заметки проекта. **Поколение индекса, построенное при заданном префиксе, один раз пересобирается**, прежнее отвечает до его публикации; если префиксы не задавались, индекс не меняется. |
